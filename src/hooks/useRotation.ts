const useRotation = () => {
    const hz = 1/ 60
    let m = 1;
    const calAngularMomentum = () => {
        // L = r x P (변위 X 선운동량)
        // P = m * v (질량 * 속도)
        // L = m * (r X v)
        // 각 속도 => rad / s
    }
}